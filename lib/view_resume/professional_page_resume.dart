import 'package:flutter/material.dart';

class ProfessionalResumePage extends StatelessWidget {
  const ProfessionalResumePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
