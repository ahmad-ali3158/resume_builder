import 'package:flutter/material.dart';

class PremiumResumePage extends StatelessWidget {
  const PremiumResumePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
