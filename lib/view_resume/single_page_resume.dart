import 'package:flutter/material.dart';

class SinglePageResume extends StatelessWidget {
  const SinglePageResume({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
