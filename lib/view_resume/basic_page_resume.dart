import 'package:flutter/material.dart';


class BasicPageResume extends StatelessWidget {
  const BasicPageResume({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
