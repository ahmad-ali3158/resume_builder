import 'package:flutter/material.dart';

class CountryResumePage extends StatelessWidget {
  const CountryResumePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
